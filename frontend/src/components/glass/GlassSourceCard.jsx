// Re-exported under the Glass naming convention — the implementation
// (expand/collapse, page number only when real, retrieval distance) lives
// in components/chat/Citation.jsx, restyled onto the glass surface.
export { default } from '../chat/Citation'
